# Lambda-only dependencies — no chromadb, no streamlit
# These are pure-Python or have manylinux wheels → safe for Amazon Linux
boto3>=1.34.0
fastapi>=0.111.0
mangum>=0.17.0
pydantic>=2.0.0
numpy>=1.26.0
clickhouse-connect>=0.7.0
python-dotenv>=1.0.0
temporalio>=1.7.0             # Temporal SDK — start workflows + send signals from Lambda
